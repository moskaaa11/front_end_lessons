//first 
let radius= prompt ('Назви периметр кола');

let pi = 3.14;

alert (`Його площа дорівнює ${pi* (radius**2) }`); 
//second

let dist = prompt ('Назви відстань між містами');

let hour = prompt ('За скільки годин ти хочеш туди дійти?');

alert (`Тобі потрібно рухатись зі швидкістю ${dist/hour} км/год`);

//third

let dollar = prompt ('Веди кількість долларів');

const euro = 1.1;

alert (`Ти отримаєш ${dollar*euro} євро`);